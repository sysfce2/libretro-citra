.core-defs:
  variables:
    JNI_PATH: .
    CORENAME: citra
    API_LEVEL: 21
    BASE_CORE_ARGS: -DENABLE_TESTS=OFF -DENABLE_DEDICATED_ROOM=OFF -DENABLE_SDL2=OFF -DENABLE_QT=OFF -DENABLE_WEB_SERVICE=OFF -DENABLE_SCRIPTING=OFF -DENABLE_OPENAL=OFF -DENABLE_LIBUSB=OFF -DCITRA_ENABLE_BUNDLE_TARGET=OFF -DCITRA_WARNINGS_AS_ERRORS=OFF -DCMAKE_POLICY_VERSION_MINIMUM=3.5
    CORE_ARGS: ${BASE_CORE_ARGS}

variables:
  STATIC_RETROARCH_BRANCH: master
  GIT_SUBMODULE_STRATEGY: recursive

# Inclusion templates, required for the build to work
include:
  ################################## DESKTOPS ##############################  ##
  # Windows 64-bit
  - project: 'libretro-infrastructure/ci-templates'
    file: '/windows-cmake-mingw.yml'

  # Linux 64-bit
  - project: 'libretro-infrastructure/ci-templates'
    file: '/linux-cmake.yml'

  # MacOS ARM 64-bit
  - project: 'libretro-infrastructure/ci-templates'
    file: '/osx-cmake-arm64.yml'

  ################################## CELLULAR ##############################  ##
  # Android
  - project: 'libretro-infrastructure/ci-templates'
    file: '/android-cmake.yml'

  ################################## CONSOLES ##############################  ##

  # Nintendo Switch
  - project: 'libretro-infrastructure/ci-templates'
    file: '/libnx-static.yml'

# Stages for building
stages:
  - build-prepare
  - build-shared
  - build-static

##############################################################################
#################################### STAGES ##################################
##############################################################################
#
################################### DESKTOPS #################################
# Windows 64-bit
libretro-build-windows-x64:
  extends:
    - .core-defs
    - .libretro-windows-cmake-x86_64
  variables:
    EXTRA_PATH: bin/Release

# Linux 64-bit
libretro-build-linux-x64:
  extends:
    - .core-defs
    - .libretro-linux-cmake-x86_64
  image: $CI_SERVER_HOST:5050/libretro-infrastructure/libretro-build-amd64-ubuntu:backports
  variables:
    CORE_ARGS: ${BASE_CORE_ARGS} -DCMAKE_C_COMPILER=gcc-11 -DCMAKE_CXX_COMPILER=g++-11

# Linux ARM 64-bit
libretro-build-linux-aarch64:
  extends:
    - .core-defs
    - .libretro-linux-cmake-aarch64
  # No image override: unlike the x64 image, the aarch64 one already ships a
  # modern GCC (12, from ppa:ubuntu-toolchain-r/test in its Dockerfile), and it
  # has no :backports tag at all. GCC 11 is not packaged there, so use 12.
  variables:
    CORE_ARGS: ${BASE_CORE_ARGS} -DCMAKE_C_COMPILER=gcc-12 -DCMAKE_CXX_COMPILER=g++-12

# MacOS ARM 64-bit
libretro-build-osx-arm64:
  extends:
    - .core-defs
    - .libretro-osx-cmake-arm64
  variables:
    CORE_ARGS: ${BASE_CORE_ARGS} -DCMAKE_POLICY_VERSION_MINIMUM=3.5

################################### CELLULAR #################################
# Android ARMv8a
android-arm64-v8a:
  extends:
    - .libretro-android-cmake-arm64-v8a
    - .core-defs
  variables:
    ANDROID_NDK_VERSION: 29.0.14206865
    NDK_ROOT: /android-sdk-linux/ndk/$ANDROID_NDK_VERSION

# Android 64-bit x86
#
# Held to NDK 26 on purpose. This is the ABI that compiles dynarmic's x64
# backend, and the mcl headers it pulls in do not survive the clang in NDK 29:
# "implicit instantiation of undefined template
# mcl::mp::detail::lift_sequence_impl". arm64-v8a never reaches that code,
# which is why it is fine on 29. 26.2 is also a version the template leaves
# alone - it rewrites a request for 27.3 into 29.
android-x86_64:
  extends:
    - .libretro-android-cmake-x86_64
    - .core-defs
  variables:
    ANDROID_NDK_VERSION: 26.2.11394342
    NDK_ROOT: /android-sdk-linux/ndk/$ANDROID_NDK_VERSION

################################### CONSOLES #################################

# libretro-build-libnx-aarch64:
#   extends:
#     - .core-defs
#     - .libretro-libnx-static-retroarch-master
